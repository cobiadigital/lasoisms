{
  "name": "lassoisms",
  "version": "1.0.0",
  "private": true,
  "description": "Tap for a random Ted Lasso quote, filtered by character.",
  "scripts": {
    "build": "echo \"static assets only — nothing to build\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
